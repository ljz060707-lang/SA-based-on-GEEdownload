# 工作记录 2026-03-31 (Tue)

**工作时间**: ~3 小时（RunPod 推理 + workspace 优化 + benchmark 首跑）

## 1. Batch 004 推理

### 概述

在 RunPod RTX 5090 上用 V3-C 权重完成 batch 004 全部 37 grids 的推理，6 进程并行，tiles 加载到 `/dev/shm` 内存加速。

### 推理配置

- 模型: V3-C (targeted HN, `exp003_C_targeted_hn/best_model.pth`)
- 后处理阈值: post_conf ≥ 0.85
- 并行度: 6 进程
- Tiles: `/dev/shm/tiles` (17.5 GB, 从 network volume 拷贝)
- 总耗时: ~20 分钟 (10:16 ~ 10:35 UTC)

### 推理结果

| 指标 | 数值 |
|------|------|
| Grids | 37 |
| 总预测数 | 3,322 |
| 平均 confidence | 0.95+ |
| 最大 grid | G1971 (405), G1855 (385), G2029 (280) |
| 最小 grid | G2034 (2), G2035 (3) |

Results 已全部同步到本地 `/mnt/d/ZAsolar/results/`。

---

## 2. Workspace 安全与优化

### 概述

基于 Codex 审计报告，独立评估后选择性处理 4/10 项 findings。重点修复凭证泄露和路径不一致问题。

### 完成项

| 问题 | 修复 | 文件 |
|------|------|------|
| S3 凭证明文硬编码 | 改为环境变量 + `.env` | `scripts/upload_to_runpod.sh` |
| `.gitignore` 缺凭证排除 | 添加 `.env*`, `*.credentials` | `.gitignore` |
| 双目录 split-brain (zasolar vs ZAsolar) | 统一为 `${WORKSPACE:-/workspace/ZAsolar}` | `cloud_setup.sh`, `scripts/runpod_init.sh`, `scripts/analysis/batch_inference.sh` |
| docs/architecture.md 过时路径 | 更新 cleaned/、benchmark、RunPod 脚本 | `docs/architecture.md` |

### 不做的项及理由

- detect_and_evaluate.py 3×zonal_stats 合并: 收益 ~10min/37grids，改动影响结果可比性
- benchmark 并行化: 跑的频率太低
- train.py GeoTIFF 重开: DataLoader workers 已流水线化，不是瓶颈
- GPU admission control: 6 进程 18GB/32GB，余量充足

### 新增脚本

| 文件 | 说明 |
|------|------|
| `scripts/runpod_init.sh` | Pod 初始化（补装 GIS 依赖，验证环境） |
| `scripts/sync_from_runpod.sh` | SSH rsync 同步 results/tiles 到本地 D 盘 |
| `.env.example` | 凭证模板 |
| `.claude/rules/04-runpod-ssh.md` | 自动更新 .env SSH 配置的规则 |

### S3 Key 已 Rotate

旧 key 在 git history 中暴露，已在 RunPod 控制台重新生成，新 key 存储在 `.env`（gitignored）。

---

## 3. Benchmark 首次实跑

### 概述

在 RunPod 上首次运行 `run_benchmark.py`，对比 V3-C (targeted HN) 和 V3-A (no HN)，验证 benchmark pipeline 端到端可用性。

### Smoke Suite 结果 (3 grids, SAM2 GT, post_conf≥0.85)

| Model | Precision | Recall | F1 | TP | FP | FN |
|-------|-----------|--------|------|----|----|-----|
| V3-C-HN | **77.5%** | 62.9% | **69.5%** | 299 | **87** | 176 |
| V3-A | 72.7% | **65.7%** | 69.0% | 312 | 117 | 163 |

### 发现的问题

1. **f-string 格式化 bug** (line 689): `f"{f1:.1% if f1 is not None else '-'}"` — 已修复
2. **`--collect-only` 路径不匹配**: `run_id` 总是追加时间戳，导致无法复用旧 run — 待修
3. **串行推理慢**: 3 grids × 2 models = ~12 分钟，需要加 `--jobs` 并发 — 待修
4. **RunPod 上 model_registry 路径**: 相对路径 vs 绝对路径不匹配 — 临时用绝对路径解决

---

## 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `scripts/upload_to_runpod.sh` | 修改 | 凭证→环境变量，修复 zasolar→ZAsolar |
| `scripts/runpod_init.sh` | 新增 | Pod 环境初始化脚本 |
| `scripts/sync_from_runpod.sh` | 新增 | 从 RunPod 同步 results/tiles |
| `.env.example` | 新增 | 凭证模板（S3 + SSH） |
| `.env` | 新增 | 实际凭证（gitignored） |
| `.gitignore` | 修改 | 添加 `.env*`, `*.credentials` |
| `cloud_setup.sh` | 修改 | 路径统一 ZAsolar |
| `scripts/analysis/batch_inference.sh` | 修改 | 路径统一 ZAsolar |
| `scripts/analysis/run_benchmark.py` | 修改 | 修复 f-string bug |
| `docs/architecture.md` | 修改 | 更新 cleaned/、benchmark、RunPod 脚本路径 |
| `.claude/rules/04-runpod-ssh.md` | 新增 | SSH 配置自动更新规则 |

## 待办

- [ ] Review batch 004 的 3,322 个 predictions
- [ ] Benchmark: 修复 `--collect-only` 路径匹配 + 加 `--jobs` 并发
- [ ] Benchmark: 在 primary suite (26 grids) 上跑完整对比
- [ ] 导出 COCO v4 数据集 → 下一轮训练
